spring.application.name=solar-watch

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/solarwatch
spring.datasource.username=postgres
spring.datasource.password=Pds56ghj
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# API Keys
geo.api.key=${GEO_API_KEY:}

# Alternative way to set the API key (uncomment and set your key here if needed)
# geo.api.key=your_api_key_here

# Enable debug logging for troubleshooting
logging.level.com.codecool.solarwatch=DEBUG
logging.level.org.springframework.web.reactive.function.client=DEBUG
